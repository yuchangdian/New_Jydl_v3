# 宽频谐波页面转换说明

- 参照 `CommonSetting_PrimarySystem_Set.ui` 转 `CommonSetting_PrimarySystem_Set.ets` 的处理方式，将 `E:\baidu\arkTS_code\qt_ui\broadband_harmonic.ui` 转成 ArkTS 页面 `entry/src/main/ets/pages/broadband_harmonic.ets`。
- 页面保留了 Qt 原界面的顶部标题栏、中部滚动区、底部操作栏三段结构。
- 中部内容按原 UI 复刻为 1 个“工频分量”区块和 9 个“谐波分量”区块，每个区块都保留“电压/电流 + 频率/幅值/相位/含有率”的表头以及 A/B/C 三相数据行。
- 保留了原界面 B 相行的灰底高亮表现。
- 按本次要求，底部“关闭”“返回”按钮改成了更圆润的圆角按钮。
- 后续迭代中，参照 `CommonSetting_PrimarySystem_Set.ets` 的字号基准，进一步上调了宽频谐波页面的副标题、区块标题、表头和数据字号，并同步增大行高与底部按钮高度。
- 在 `entry/src/main/ets/components/TopMenu.ets` 中补上了“电能质量 -> 谐波分析”到 `pages/broadband_harmonic` 的跳转，同时保留“实时指标”到 `pages/quality_index` 的直达跳转。
- 在 `entry/src/main/resources/base/profile/main_pages.json` 中注册了新页面。
