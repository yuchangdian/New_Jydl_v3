# 撤销 add 测试函数适配说明 2026-03-16

本次按用户要求，只撤销上一轮为了适配 `add` 测试函数所做的修改，不处理别的 native 模块加载问题。

已撤销内容：

1. `entry/src/main/ets/entryability/EntryAbility.ets`
   - 恢复 `import testNapi from 'libentry.so';`
   - 删除 `TcpClientModule` 里的可选 `add`
   - 删除 `logNativeAddResult()`
   - 恢复 `onCreate()` 里直接调用 `testNapi.add(2, 3)` 的写法

2. `entry/src/mock/Libentry.mock.ets`
   - 删除新增的 `add` mock 导出

3. `entry/src/main/ets/pages/Index.ets`
   - 恢复 `import testNapi from 'libentry.so';`
   - 去掉多余空行

4. 删除文档
   - `md/add测试函数接入说明-20260316.md`

说明：

- 这次没有回退 `entry/src/main/cpp/napi_init.c`
- 这次没有做编译验证
- 当前工程里其它和 OpenHarmony 或 native 模块加载相关的问题，仍需单独排查
