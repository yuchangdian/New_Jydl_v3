# quality_index 界面转换说明

## 本次处理

- 参照 `CommonSetting_PrimarySystem_Set.ui -> CommonSetting_PrimarySystem_Set.ets` 的页面拆法，新建了 `entry/src/main/ets/pages/quality_index.ets`。
- 按 `quality_index.ui` 的原始结构保留了三段式布局：
  - 顶部标题栏
  - 中部滚动指标区
  - 底部操作栏
- `quality_index.ui` 中三条深灰底行已保留：
  - A/B/C 相频率偏差值
  - A/B/C 相电压偏差值
  - AB/BA/CA 相电压偏差值
- 底部按钮按要求做得更圆润，当前圆角为 `18`。

## 跳转接入

- 已在 `TopMenu.ets` 中把二级菜单“实时指标”接到 `pages/quality_index`。
- 已在 `main_pages.json` 注册 `pages/quality_index`。

## 说明

- 这次先按“原封不动改成 ets”的要求，页面里的文案和值基本按 `.ui` 原样保留。
- `quality_index.ui` 里原本就是空值的字段，这次也继续保留为空，未额外补业务逻辑。
- 编译验证已通过，当前没有新增构建错误。

## 2026-03-17 二次迭代

- 按 `CommonSetting_PrimarySystem_Set.ets` 的字号层级把 `quality_index.ets` 字号整体调大。
- 主要调整：
  - 标题副标题 `16 -> 18`
  - 分组标题 `16 -> 18`
  - 指标标签和值 `16 -> 18`
  - 底部按钮和时间 `16 -> 18`
- 为避免放大后拥挤，同时把指标行高 `52 -> 56`，底部按钮高 `50 -> 55`，分组箭头图标 `22 -> 24`。

## 2026-03-17 三次迭代

- 继续把 `quality_index.ets` 字号上调一档：
  - 主标题 `20 -> 22`
  - 副标题 `18 -> 20`
  - 分组标题 `18 -> 19`
  - 指标标签和值 `18 -> 19`
- 顶部标题改成单个文本 `电能质量—实时指标`，强制单行显示，不再拆成两段。
- 中部滚动内容增加右侧留白，当前右边距为 `88`。
- 为适配更大的字，指标行高同步调整为 `60`。

## 2026-03-17 四次迭代

- 顶部标题改成真正居中实现，不再依赖左右权重挤压。
- 当前实现方式：
  - 标题放在 `Stack` 的中心层
  - 左右两侧各预留同宽区域
  - 返回/关闭按钮只占右侧预留区，不参与标题居中计算
- 这样标题视觉中心和页面中心一致。
