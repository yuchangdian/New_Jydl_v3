# 程序未完成项盘点总结

## 1. 当前总体状态

1. 当前工程可以成功编译，`assembleHap` 已通过。
2. 主要的 ArkTS 页面已经覆盖了首页、软压板、保护定值整定/浏览选择、电能质量实时指标/谐波分析、若干“定值管理-整定”页面、密码设置、开出测试、开入测试。
3. 但从“功能完成度”看，项目仍处于“主干页面已搭出、很多功能还没有接实”的阶段。

## 2. 明确还没有完成的部分

### 2.1 菜单还有大量入口没有接页面

当前菜单定义在 `entry/src/main/ets/components/TopMenu.ets`，但实际只接了少量入口。

已接入口：

1. 运行操作 -> 压板投退
2. 电能质量 -> 实时指标
3. 电能质量 -> 谐波分析
4. 电能质量 -> 告警定值 -> 整定
5. 定值管理 -> 一次系统/模拟量/遥测参数/遥信参数/遥控参数/越限报警/统计参数 -> 整定
6. 定值管理 -> 保护定值 -> 浏览/整定
7. 装置测试 -> 开出测试
8. 装置测试 -> 开入测试
9. 装置设置 -> 密码设置

未接入口：

1. 运行操作 -> 定值切换
2. 运行操作 -> 遥控操作
3. 电能质量 -> 告警定值 -> 浏览
4. 高级分析 -> 故障特征
5. 高级分析 -> 负荷特征
6. 高级分析 -> 风险评估
7. 定值管理 -> 一次系统/模拟量/遥测参数/遥信参数/遥控参数/越限报警/统计参数 -> 浏览
8. 定值管理 -> 保护定值 -> 拷贝
9. 定值管理 -> 保护定值 -> 删除
10. 报告管理 -> 全部未接
11. 打印操作 -> 全部未接
12. 装置测试 -> 灯光测试
13. 装置测试 -> 精度测试
14. 装置测试 -> 零漂测试
15. 装置测试 -> 远动测试
16. 装置测试 -> 试验测试
17. 装置设置 -> 时间设置
18. 装置设置 -> 对时设置
19. 装置设置 -> 通信设置
20. 装置设置 -> 液晶设置
21. 装置设置 -> 电度量
22. 装置设置 -> 模拟量

结论：菜单结构已经很完整，但大部分业务入口还只是“菜单定义完成”，并没有落到页面。

### 2.2 还有 8 个 Qt 浏览页没有转换成 ArkTS

对照 `E:/baidu/arkTS_code/qt_ui` 和 `entry/src/main/ets/pages`，以下 `.ui` 还没有对应的 ArkTS 页面：

1. `CommonSetting_AnalogQuantity_Disp.ui`
2. `CommonSetting_ExceedingLimit_Disp.ui`
3. `CommonSetting_PowerQuality_Disp.ui`
4. `CommonSetting_PrimarySystem_Disp.ui`
5. `CommonSetting_Statistics_Disp.ui`
6. `CommonSetting_TeleControlling_Disp.ui`
7. `CommonSetting_TeleMeasuring_Disp.ui`
8. `CommonSetting_TeleSignaling_Disp.ui`

这和上面的菜单缺口是对应的：定值管理里“浏览”一侧目前基本都还没有真正完成。

### 2.3 首页和部分页面仍然是静态占位数据

以下地方仍然明显是演示/占位值，没有接真实数据源：

1. `entry/src/main/ets/pages/Index.ets`
   里面大量值还是 `**.**Hz`、`**.**A`、`**.**kW`、`**.**V∠**.**°`
2. `entry/src/main/ets/pages/Index.ets`
   页面的时间还是固定写死的 `2024/7/7 12:12:12`
3. `entry/src/main/ets/pages/quality_index.ets`
   时间是固定的 `2024/12/30 12:12:30`
4. `entry/src/main/ets/components/SoftStrapPanel.ets`
   日期时间也是固定值
5. 绝大多数设置页底部时间栏仍然是固定 `2024/12/30 12:12:30`

结论：目前界面框架已经能跑，但“实时数据刷新”和“真实时钟”还没有统一接入。

### 2.4 新做的开出/开入测试页，目前也还是界面层完成，业务层未接

#### 开出测试页

文件：`entry/src/main/ets/pages/DigitalOutput_Test.ets`

当前状态：

1. 勾选项点击后只是在本地切换状态
2. “确认”按钮只会把状态保存到本地 JSON
3. 还没有任何实际开出命令、设备通讯或联动逻辑
4. 页面标题副标题目前仍然是 `开入测试`，这和页面名 `DigitalOutput_Test` 不一致，属于明显待修正项

#### 开入测试页

文件：`entry/src/main/ets/pages/DigitalInput_Test.ets`

当前状态：

1. 页面只是把开入项按 UI 摆出来
2. 所有状态默认都是未选中
3. 没有定时刷新、没有设备数据读取、没有状态订阅
4. 这页本质上还是静态展示页

结论：这两个页面现在完成的是“界面转换”，不是“测试功能完成”。

## 3. 明确存在的技术债

### 3.1 工程里还有未用占位组件

文件：`entry/src/main/ets/components/TopTitleBar.ets`

问题：

1. 文件里还保留了 `TODO: 添加刷新逻辑`
2. 文件里还保留了 `TODO: 添加关闭逻辑`
3. 当前代码库里没有实际使用这个组件

结论：它更像早期试验组件，建议后续要么真正接入，要么删除，避免误导。

### 3.2 还有原型页面残留

文件：`entry/src/main/ets/pages/new_file.html`

问题：

1. 文件里保留了整套旧原型/静态 HTML
2. 内容里仍然有大量占位数值
3. 当前 ArkTS 页面已经逐步替代它，但文件仍留在正式 pages 目录中

结论：这是明显的原型残留文件，建议后续清理或移出正式目录。

### 3.3 仍有较多 API 弃用告警

本次编译还能通过，但一直有以下告警：

1. `router.pushUrl` 已弃用
2. `router.back` 已弃用
3. `getContext` 已弃用

结论：这不是“当前阻塞项”，但继续堆下去，后面升级 SDK 时会增加改造成本。

## 4. 建议的完成顺序

### 第一优先级

1. 把 `定值管理` 下所有“浏览”页补齐
2. 把 `电能质量 -> 告警定值 -> 浏览` 补齐
3. 修正 `DigitalOutput_Test.ets` 标题显示错误

原因：

1. 这些和现有菜单最贴近
2. 对当前已有页面体系改动最小
3. 用户操作链条最容易闭环

### 第二优先级

1. 给首页、实时指标页、软压板区域接真实时间
2. 把首页和实时指标页的占位数据替换成真实数据或模拟数据源
3. 给开入测试页接状态刷新
4. 给开出测试页接真实确认动作

原因：

1. 这部分决定程序看起来是不是“真在工作”
2. 不解决的话，页面虽然多，但整体会一直像静态样机

### 第三优先级

1. 补装置测试其余页面
2. 补报告管理
3. 补打印操作
4. 补装置设置剩余页面
5. 清理 `TopTitleBar.ets` 和 `new_file.html`
6. 逐步替换弃用 API

## 5. 一句话结论

当前程序不是“没法用”，而是“主干框架已经成形，但大量菜单项、浏览页、实时数据和设备交互还没有真正完成”。
