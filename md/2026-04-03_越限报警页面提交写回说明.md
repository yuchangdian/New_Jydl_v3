# 越限报警页面提交写回说明

## 本次目标
- 保留 `CommonSetting_ExceedingLimit_Set.ets` 原业务页面布局。
- 点击“提交”后：
  - 弹出“等待回应”提示框。
  - 将界面参数写入 `data.cpp` 中的 `CommonSetting_ExceedingLimit_Struct ExceedingLimitSetting`。

## 本次实现
- 页面继续沿用原有的 native 读取链路：
  - `getExceedingLimitSetting()`
  - 页面打开时优先读取 native 结构体并回填到 `CommonSettingExceedingLimitData`。

- 页面新增 native 写回链路：
  - `updateExceedingLimitSetting(value)`
  - 在 ArkTS 侧把界面字段映射成 `CommonSettingExceedingLimitWriteValue`。
  - `投入 -> 1`，`退出 -> 0`。
  - 数值字段用 `parseFloat`，延时字段写回时转为非负整数。

- 提交流程改为：
  1. 先保存本地 JSON。
  2. 再调用 `updateExceedingLimitSetting(...)` 写入 native 结构体。
  3. 成功后弹出“等待回应”弹窗。
  4. 失败时仍走原提示弹窗。

## 验证
- 已执行增量构建：
  - `assembleHap --incremental`
- 结果：
  - `BUILD SUCCESSFUL`
