# 2026-04-15 Index 压板仅字号放大记录

## 修改目的

按反馈仅把 `Index.ets` 页面里的软压板、硬压板文字调大一点，不调整任何间距、留白、复选框尺寸和布局比例。

## 本次调整

- 仅修改 `entry/src/main/ets/pages/Index.ets`
- 压板标题字号从 `18` 调整到 `19`
- 压板条目字号从 `16` 调整到 `17`
- 不改动 `margin`、`padding`、复选框大小、列布局和容器结构

## 影响范围

- 只影响 `Index.ets` 中软压板、硬压板区域的文字大小
- 不改动首页其它区域和数据逻辑
