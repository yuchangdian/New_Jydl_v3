# add 测试函数最小适配说明 2026-03-16

本次按“不要改太多”的要求，只做了最小范围调整，让 ArkTS 侧调用 `add` 时不再依赖默认导入对象。

改动如下：

1. `entry/src/main/ets/entryability/EntryAbility.ets`
   - 删除 `import testNapi from 'libentry.so';`
   - 在 `TcpClientModule` 接口里补上可选 `add`
   - `onCreate()` 改为复用现有 `loadTcpClientModule()`，拿到模块后再调用 `add(2, 3)`
   - 如果模块已加载但没有 `add`，打印 `add 测试函数不可用`

2. `entry/src/mock/Libentry.mock.ets`
   - 补充 `add(a, b)`，返回 `a + b`

3. `entry/src/main/ets/pages/Index.ets`
   - 删除未使用的 `import testNapi from 'libentry.so';`

说明：

- `entry/src/main/cpp/napi_init.c` 当前本身已经导出了 `add`，这次没有改 native 代码
- `entry/src/main/cpp/types/libentry/Index.d.ts` 当前本身已经声明了 `add`，这次没有改类型声明

验证结果：

- 代码层面的最小适配已经完成
- 本机构建仍被环境拦住：`00303168 Configuration Error / SDK component missing`
- 所以这次还没有完成最终编译验证
