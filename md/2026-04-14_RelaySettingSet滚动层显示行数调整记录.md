# 2026-04-14 RelaySettingSet 滚动层显示行数调整记录

## 修改目的

把 `RelaySettingSet.ets` 中间滚动区域的显示密度调低一些，让单屏可见内容从当前大约 12 行，收敛到更接近 9 行左右，阅读和点按都更宽松。

## 本次调整

- 仅调整 `entry/src/main/ets/pages/RelaySettingSet.ets` 的正文排版参数
- 区块标题字号从 `18` 调整到 `19`
- 标签字号从 `18` 调整到 `19`
- 输入值字号从 `16` 调整到 `18`
- 单位字号从 `18` 调整到 `19`
- 正文字号最小值从 `15` 调整到 `16`
- 输入框高度从 `34` 调整到 `42`
- 列表上下内边距略微增大
- 区块标题与正文之间的间距从 `4` 调整到 `6`
- 行间隔从 `3` 调整到 `4`
- 区块上下内边距从 `6` 调整到 `8`
- 区块底部外边距从 `6` 调整到 `8`
- 单行最小高度从 `34` 调整到 `44`

## 影响范围

- 只影响保护定值整定页中间滚动层的显示密度
- 不改动页面数据读取、提交、自动刷新和底部按钮逻辑
