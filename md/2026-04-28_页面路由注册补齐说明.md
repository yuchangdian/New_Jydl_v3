# 页面路由注册补齐说明

## 本次处理

已在 `entry/src/main/resources/base/profile/main_pages.json` 中补齐当前源码里缺失的页面注册：

- `AlarmSettings/index`
- `pages/meter/index`
- `pages/pqInsight/index`
- `pages/pqInsight/EventCenter`

## 处理依据

- `TopMenu.ets` 中存在对以下页面的直接跳转：
  - `pages/meter/index`
  - `pages/pqInsight/index`
  - `AlarmSettings/index`
  - `pages/pqInsight/EventCenter`
## 额外修正

- `AlarmSettings` 页面真实位置是 `entry/src/main/ets/AlarmSettings/index.ets`
- 原菜单跳转写成了 `pages/AlarmSettings/index`，与真实文件路径不一致
- 已同步改为 `AlarmSettings/index`，否则即使注册页面也仍然无法正确跳转

## 暂未纳入

- `pages/quality/broadband`
- 这页当前没有菜单入口，而且会继续带出一整组质量分析子页的额外依赖
- 本次先不纳入页面注册，避免把与“表计/波形/事件/告警”无关的半成品页面一起编进来

## 预期结果

- 点击顶部菜单中的“表计”应可正常进入页面。
- 同类二级目录页面不再因为未注册而出现“点击无反应”的现象。

## 说明

- 本次只补页面注册，不调整业务逻辑。
- 若设备上运行的还是旧包，还需要重新构建并重新安装后才能看到效果。
