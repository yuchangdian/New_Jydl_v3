# 2026-04-15 RelaySettingSet 字号再放大记录

## 修改目的

根据反馈，把 `RelaySettingSet.ets` 页面里的文字再放大一点，让中间定值内容、标题和底部时间的可读性更好。

## 本次调整

- 仅修改 `entry/src/main/ets/pages/RelaySettingSet.ets`
- 提示文字字号从 `18` 调整到 `19`
- 底部时间字号从 `18` 调整到 `19`
- 顶部标题字号从 `20` 调整到 `21`
- 分组标题字号从 `19` 调整到 `20`
- 标签字号从 `19` 调整到 `20`
- 输入值字号从 `18` 调整到 `19`
- 单位字号从 `19` 调整到 `20`
- 最小缩放字号从 `16` 调整到 `17`
- 输入框高度从 `42` 调整到 `44`

## 影响范围

- 只影响 `RelaySettingSet.ets` 的显示效果
- 不改动数据读取、提交、自动刷新和路由逻辑
