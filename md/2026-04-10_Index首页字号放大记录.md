# Index 首页字号放大记录

## 本次调整

- 只修改了 `entry/src/main/ets/pages/Index.ets`。
- `TopMenu()` 菜单栏没有改动。
- 首页左侧状态信息、参数列表、右侧电压电流展示区、硬压板和软压板标题与条目文字整体放大一档。

## 具体处理

- 新增首页局部字号和宽度常量，避免继续在页面里散落写死值。
- 左侧面板主标题从 `20` 调到 `24`，普通文字统一调到 `18`。
- 右侧分组标题从 `22` 调到 `26`，相量和数值文字从 `20` 调到 `22`。
- 压板区域标题从 `15` 调到 `18`，压板条目文字从 `14` 调到 `16`。
- 同步增大了左侧面板宽度、参数值宽度、右侧标题列宽和数值列宽，避免字号放大后内容拥挤。

## 验证

- 已执行增量构建 `assembleHap --analyze=normal --parallel --incremental`。
- 命令退出码为 `0`，本次修改没有引入新的编译错误。
- 仍有工程内原本存在的 ArkTS 过时 API 警告，本次未处理。

## 第二轮继续放大

- 按“铺满界面”的方向，又对 `entry/src/main/ets/pages/Index.ets` 做了一轮放大。
- 这次仍然只调整首页正文区，`TopMenu()` 菜单栏继续保持不动。
- 左侧状态面板继续加宽，标题、正文和参数值区域再次放大，并把行间距和上下留白一起拉开。
- 右侧电压、电流展示区进一步增大了分组标题、相量数值、标签列宽和内容列宽，同时增大滚动区内边距。
- 硬压板和软压板区域同步增大标题、条目字号和各组之间的横向间距，让内容更接近铺满 1280×720 界面。

## 本轮微调

- 左侧 `LeftPanel()` 没有再改字号，只把面板内部的下边距进一步调大，底部留白更明显。
- 右侧电压/电流展示区没有改内容排布，只把整体顶部内边距缩小了一点，让上方空隙更紧凑。
