# 2026-04-14 CommonSetting_AnalogQuantity 正文放大记录

## 修改目的

把 `CommonSetting_AnalogQuantity_Set.ets` 中间正文区域的文字调大一点，让模拟量参数表格更清楚，同时不改顶部标题区和底部按钮区的字号。

## 本次调整

- 表头字号从 `18` 调整为 `20`
- 左右两侧参数标签字号从 `16` 调整为 `18`
- 输入框数值字号从 `16` 调整为 `18`
- 单位字号从 `16` 调整为 `18`
- 表头高度从 `42` 调整为 `48`
- 每行高度从 `48` 调整为 `54`
- 输入框高度从 `35` 调整为 `40`
- 输入框宽度从 `76` 调整为 `82`
- 单位宽度从 `16` 调整为 `20`
- 输入框与单位间距从 `2` 调整为 `4`
- 单组字段宽度从 `94` 调整为 `106`
- 标签单元格左右内边距从 `6` 调整为 `8`

## 影响范围

- 仅修改 `entry/src/main/ets/pages/CommonSetting_AnalogQuantity_Set.ets`
- 仅影响页面中间正文表格区域的显示密度和字号
- 不涉及菜单、页面跳转、提交逻辑、底部按钮逻辑和原生模块调用

## 二次调整

用户反馈正文偏大，而且希望单个界面直接显示完全，不要出现滑动。

本次进一步收口为“一屏显示优先”的方案：

- 表头字号从 `20` 回调到 `19`
- 参数标签、输入值、单位字号从 `18` 回调到 `17`
- 表头高度从 `48` 回调到 `40`
- 每行高度从 `54` 回调到 `50`
- 输入框高度从 `40` 回调到 `36`
- 输入框宽度从 `82` 回调到 `80`
- 单位宽度从 `20` 回调到 `18`
- 输入框与单位间距从 `4` 回调到 `3`
- 单组字段宽度从 `106` 回调到 `101`
- 标签单元格左右内边距从 `8` 回调到 `7`
- 正文最小高度从 `617` 调整到 `450`
- 正文底部留白从 `200` 调整到 `10`

这样正文仍然比原始版本略大，但优先保证在当前目标分辨率下尽量一屏显示完成，减少滚动。
