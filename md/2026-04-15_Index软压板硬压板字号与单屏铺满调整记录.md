# 2026-04-15 Index 软压板硬压板字号与单屏铺满调整记录

## 修改目的

把 `Index.ets` 页面里的软压板、硬压板文字再放大一点，同时把这两个区域的布局做得更饱满一些，尽量更贴近单屏铺满显示。

## 本次调整

- 仅修改 `entry/src/main/ets/pages/Index.ets`
- 硬压板、软压板标题字号从 `20` 调整到 `22`
- 压板条目字号从 `17` 调整到 `19`
- 新增压板复选框尺寸常量，复选框统一放大到 `24`
- 新增压板条目最小高度常量，单条最小高度设为 `40`
- 硬压板标题右侧和各条目之间的横向间距重新收口，避免放大后过挤
- 硬压板区块增加上下内边距，并把整体上边距从 `20` 调整到 `22`
- 软压板首行增加上下内边距，首行条目横向间距同步调整
- 软压板四列内容里的复选框统一放大，并给每条设置最小高度
- 软压板第三列顶部补偿从 `42` 调整到 `48`，配合更大的字号后保持列间视觉平衡
- 软压板内容区顶部和底部留白略微增大，让右侧区域更接近铺满单屏

## 影响范围

- 只影响 `Index.ets` 里的软压板和硬压板显示样式
- 不改动首页菜单、量测值刷新、路由跳转和原生模块读取逻辑
